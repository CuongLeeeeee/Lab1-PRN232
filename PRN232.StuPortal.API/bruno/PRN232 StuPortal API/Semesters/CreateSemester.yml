info:
  name: CreateSemester
  type: http
  seq: 2
  tags:
    - Semesters

http:
  method: POST
  url: "{{baseUrl}}/api/semesters"
  body:
    type: json
    data: |-
      {
        "semesterName": "",
        "startDate": "",
        "endDate": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/semesters"
      method: POST
      body:
        type: json
        data: |-
          {
            "semesterName": "",
            "startDate": "",
            "endDate": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
