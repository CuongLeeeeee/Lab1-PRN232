info:
  name: UpdateEnrollment
  type: http
  seq: 4
  tags:
    - Enrollments

http:
  method: PUT
  url: "{{baseUrl}}/api/enrollments/:id"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "status": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/enrollments/:id"
      method: PUT
      params:
        - name: id
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "status": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
