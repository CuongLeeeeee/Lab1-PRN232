info:
  name: CreateEnrollment
  type: http
  seq: 2
  tags:
    - Enrollments

http:
  method: POST
  url: "{{baseUrl}}/api/enrollments"
  body:
    type: json
    data: |-
      {
        "studentId": 0,
        "courseId": 0,
        "enrollDate": "",
        "status": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/enrollments"
      method: POST
      body:
        type: json
        data: |-
          {
            "studentId": 0,
            "courseId": 0,
            "enrollDate": "",
            "status": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
