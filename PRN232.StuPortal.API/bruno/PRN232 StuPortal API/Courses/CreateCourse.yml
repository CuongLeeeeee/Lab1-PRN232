info:
  name: CreateCourse
  type: http
  seq: 2
  tags:
    - Courses

http:
  method: POST
  url: "{{baseUrl}}/api/v1/courses"
  body:
    type: json
    data: |-
      {
        "courseName": "",
        "semesterId": 0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/courses"
      method: POST
      body:
        type: json
        data: |-
          {
            "courseName": "",
            "semesterId": 0
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
